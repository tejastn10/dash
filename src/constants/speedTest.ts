export const DOWNLOAD_TEST_SIZES_MB = [1, 5, 10, 25];
export const MIN_DOWNLOAD_SIZE_MB = 1;
export const MAX_DOWNLOAD_SIZE_MB = 100;

export const UPLOAD_SIZE_BYTES = 5 * 1024 * 1024;

// crypto.getRandomValues max bytes per call (Web Crypto spec limit)
export const CRYPTO_CHUNK_SIZE = 65536;
